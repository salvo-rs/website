# Сжатие ответов

Промежуточное ПО для обработки сжатия содержимого `Response`.

Поддерживает три формата сжатия: `br`, `gzip`, `deflate`. Можно настроить приоритет каждого метода сжатия в соответствии с требованиями.

## Сжатие данных в протоколе HTTP

1. На уровне формата файла: определённые типы файлов используют специализированные алгоритмы сжатия.
2. На уровне протокола HTTP: сжатие ресурсов от конца к концу реализуется через кодирование содержимого.
3. На уровне соединения: сжатие данных между узлами HTTP-соединения.

Больше информации о сжатии HTTP можно найти в [документации Mozilla Developer Network](https://developer.mozilla.org/ru/docs/Web/HTTP/Guides/Compression).

## Пример кода

import { Tab, Tabs } from '@rspress/core/theme';

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/compression/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/compression/Cargo.toml"
  ```
  </Tab>
</Tabs>
{/* Auto generated, origin file hash:adfd8752c21601fac83a31d329cf6dae */}