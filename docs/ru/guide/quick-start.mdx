```russian
import { Tab, Tabs } from '@rspress/core/theme';

# Быстрый старт

## Установка Rust
Если Rust ещё не установлен, воспользуйтесь официальным установщиком (`rustup`)[https://doc.rust-lang.org/book/ch01-01-installation.html].

:::tip
Минимальная поддерживаемая версия Rust для Salvo — 1.85. Выполните `rustup update`, чтобы убедиться в совместимости вашей версии.
:::

## Создание первой программы на Salvo

Создайте новый проект:

```bash
cargo new hello --bin
```

Добавьте зависимости в `Cargo.toml`:

```toml file="<root>/codes/hello/Cargo.toml"
```

В файле `main.rs` создайте простой обработчик `hello`, выводящий текст `"Hello world"`.

```rust file="<root>/codes/hello/src/main.rs"
```

Поздравляем! Ваша первая программа Salvo готова. Запустите `cargo run` в терминале и откройте в браузере `http://127.0.0.1:5800`.

## Подробное объяснение

Здесь `hello_world` — это `Handler` для обработки запросов. Атрибут `#[handler]` позволяет функции реализовать трейт `Handler` и использовать упрощённые параметры.

- Исходная форма:

    ```rust
    #[handler]
    async fn hello(_req: &mut Request, _depot: &mut Depot, res: &mut Response, _ctrl: &mut FlowCtrl) {
        res.render("Hello world");
    }
    ```

- Неиспользуемые параметры можно опускать. Например, `_req`, `_depot` и `_ctrl` здесь не нужны:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) {
        res.render("Hello world");
    }
    ```

- Любой тип, реализующий трейт `Writer`, может быть возвращаемым значением. Например, `&str` выводит обычный текст:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> &'static str {
        "Hello world"
    }
    ```

- Часто используется `Result<T, E>` для обработки ошибок. Если `T` и `E` реализуют `Writer`, то `Result` можно возвращать:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> Result<&'static str, ()> {
        Ok("Hello world")
    }
    ```

## Стремительный HTTP3

Говорят, HTTP3 быстр как ласточка — мечта многих программистов. Теперь Salvo делает её реальностью, позволяя каждому насладиться преимуществами HTTP3!

Сначала подключите функцию HTTP3 в `Cargo.toml`, затем измените `main.rs`:

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/hello-h3/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/hello-h3/Cargo.toml"
  ```
  </Tab>
</Tabs>

## Инструмент командной строки Salvo 🛠️

[Salvo CLI](https://github.com/salvo-rs/salvo-cli) — инструмент для фреймворка Salvo. Он помогает создавать чистый код, экономя время для важных дел.

Если у вас есть идеи по улучшению CLI или вы обнаружили ошибки — создавайте issue! Мы ценим ваше мнение.

### Шаг 1

Установите CLI:

```bash
cargo install salvo-cli
```

### Шаг 2

> Создайте новый проект Salvo с помощью команды `new` и имени проекта:
>
> ```bash
> salvo new имя_проекта
> ```

Этот простой инструмент позволяет быстро настроить проект Salvo, чтобы вы могли сосредоточиться на бизнес-логике. ✨

## Другие примеры

Рекомендуем клонировать репозиторий Salvo и запустить примеры из папки `examples`. Например, так запускается пример `hello`:

```sh
git clone https://github.com/salvo-rs/salvo
cd salvo/examples
cargo run --bin example-hello
```

В папке `examples` много примеров. Запускайте их командами вида `cargo run --bin example-<имя>`.
```
{/* Auto generated, origin file hash:d6d8293cda56cad3e45ea1ac2eb6ba02 */}