# JWT Аутентификация

JWT (JSON Web Token) — это открытый стандарт (RFC 7519) для безопасной передачи информации между сторонами. Это компактный и URL-безопасный способ представления утверждений в формате JSON-объекта, которые обычно используются для аутентификации и обмена данными.

JWT состоит из трёх частей:
- **Заголовок (Header)** - определяет тип токена и используемый алгоритм подписи
- **Полезная нагрузка (Payload)** - содержит утверждения (claims), такие как ID пользователя, роли, срок действия и т.д.
- **Подпись (Signature)** - служит для проверки целостности сообщения при передаче

Типичный процесс аутентификации с использованием JWT:
1. После входа пользователя сервер генерирует JWT-токен
2. Токен возвращается клиенту и сохраняется (обычно в localStorage или cookie)
3. В последующих запросах клиент передаёт этот токен в заголовке Authorization
4. Сервер проверяет валидность токена и предоставляет доступ

Предоставляет промежуточное ПО для проверки JWT Auth.

_**Пример кода**_

import { Tab, Tabs } from 'rspress/theme';
import JwtAuthCode from '../../../../codes_md/jwt-auth/src/main.mdx';
import JwtAuthCargoCode from '../../../../codes_md/jwt-auth/Cargo.mdx';

<Tabs>
  <Tab label="main.rs">
    <JwtAuthCode/>
  </Tab>
  <Tab label="Cargo.toml">
    <JwtAuthCargoCode/>
  </Tab>
</Tabs>
{/* 本行由工具自动生成,原文哈希值:4d9196c8fb1dedaa4b7291d33747b794 */}