import { Tab, Tabs } from '@rspress/core/theme';

# Schnellstart

## Rust installieren
Falls Sie Rust noch nicht installiert haben, können Sie es mit dem offiziellen Tool (`rustup`)[https://doc.rust-lang.org/book/ch01-01-installation.html] installieren.

:::tip
Die derzeit von Salvo unterstützte Mindestversion von Rust ist 1.85. Führen Sie `rustup update` aus, um sicherzustellen, dass Sie eine kompatible Rust-Version installiert haben.
:::

## Ihr erstes Salvo-Programm schreiben

Erstellen Sie ein neues Projekt:

```bash
cargo new hello --bin
```

Fügen Sie die Abhängigkeit zur `Cargo.toml` hinzu.

```toml file="<root>/codes/hello/Cargo.toml"
```

Erstellen Sie in `main.rs` einen einfachen Funktions-Handler namens `hello`, der lediglich den Text `"Hello world"` ausgibt.

```rust file="<root>/codes/hello/src/main.rs"
```

Glückwunsch! Ihr erstes Salvo-Programm ist fertig. Führen Sie einfach `cargo run` in der Kommandozeile aus und rufen Sie dann `http://127.0.0.1:5800` in Ihrem Browser auf.

## Detaillierte Erklärung

Hier ist `hello_world` ein `Handler`, der Benutzeranfragen verarbeitet. Das Attribut `#[handler]` ermöglicht es einer Funktion, einfach das `Handler`-Trait zu implementieren. Außerdem erlaubt es uns, Funktionsparameter auf verschiedene Arten abzukürzen.

- Ursprüngliche Form:

    ```rust
    #[handler]
    async fn hello(_req: &mut Request, _depot: &mut Depot, res: &mut Response, _ctrl: &mut FlowCtrl) {
        res.render("Hello world");
    }
    ```

- Sie können unbenutzte Parameter weglassen. Da `_req`, `_depot` und `_ctrl` hier nicht verwendet werden, können sie einfach entfallen:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) {
        res.render("Hello world");
    }
    ```

- Jeder Typ kann als Rückgabetyp der Funktion dienen, solange er das `Writer`-Trait implementiert. Zum Beispiel implementiert `&str` `Writer`. Wenn es als Rückgabewert dient, gibt es einfachen Text aus:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> &'static str {
        "Hello world"
    }
    ```

- Häufiger ist der Fall, dass wir `Result<T, E>` als Rückgabetyp verwenden möchten, um Fehler während der Funktionsausführung behandeln zu können. Wenn sowohl `T` als auch `E` `Writer` implementieren, dann kann `Result<T, E>` als Rückgabewert dienen:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> Result<&'static str, ()> {
        Ok("Hello world")
    }
    ```

## Verspieltes HTTP3

Es heißt, HTTP3 sei federleicht, von vielen Entwicklern ersehnt, aber unerreicht. Diesmal macht Salvo diesen Wunsch wahr und ermöglicht es allen, mühelos in den Genuss der wunderbaren Dienste von HTTP3 zu kommen!

Aktivieren Sie zunächst die HTTP3-Funktion in `Cargo.toml` und ändern Sie dann `main.rs` wie folgt:
<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/hello-h3/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/hello-h3/Cargo.toml"
  ```
  </Tab>
</Tabs>

## Salvo CLI Tool 🛠️

Das [Salvo CLI](https://github.com/salvo-rs/salvo-cli) ist ein Werkzeug, das für das Salvo Web-Framework entwickelt wurde. Es erstellt sauberen, gut lesbaren Code und spart Ihnen Zeit für die interessanteren Dinge im Leben.

Wenn Sie Ideen zur Verbesserung des CLI haben oder Probleme entdecken, die gelöst werden müssen, zögern Sie nicht! Erstellen Sie ein Issue – wir freuen uns über Ihre Beiträge.

### Schritt 1

Installieren Sie das CLI-Tool:

```bash
cargo install salvo-cli
```

### Schritt 2

> Erstellen Sie ein neues Salvo-Projekt mit dem Befehl `new`, gefolgt von Ihrem Projektnamen:
>
> ```bash
> salvo new projektname
> ```

Mit diesem einfachen CLI-Tool können Sie schnell Salvo-Projekte starten und sich auf Ihre Geschäftslogik konzentrieren, anstatt auf den Aufbau der Projektstruktur. ✨

## Weitere Beispiele

Es wird empfohlen, das Salvo-Repository direkt zu klonen und dann die Beispiele im `examples`-Verzeichnis auszuführen. Der folgende Befehl führt beispielsweise das `hello`-Beispiel aus:

```sh
git clone https://github.com/salvo-rs/salvo
cd salvo/examples
cargo run --bin example-hello
```

Im `examples`-Verzeichnis finden sich viele Beispiele. Alle können mit einem ähnlichen Befehl wie `cargo run --bin example-<name>` ausgeführt werden.
{/* Auto generated, origin file hash:0c4a88ceb8e2d80be891a10caa082999 */}