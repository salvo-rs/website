# Grundlegende Authentifizierung

Middleware, die Unterstützung für Basic Auth bietet.

import { Tab, Tabs } from '@rspress/core/theme';

## Einführung in Basic Auth

Basic Auth ist ein einfacher HTTP-Authentifizierungsmechanismus, bei dem Benutzername und Passwort über das `Authorization`-Feld im HTTP-Request-Header übertragen werden. Das Format lautet `Authorization: Basic <base64(Benutzername:Passwort)>`. Obwohl einfach, werden die Anmeldedaten lediglich Base64-kodiert und nicht verschlüsselt, weshalb es üblicherweise in Kombination mit HTTPS verwendet werden sollte, um die Sicherheit zu gewährleisten.

## Vergleich der Basic-Auth-Implementierungen in gängigen Frameworks

| Framework | Sprache | Basic-Auth-Implementierung |
|-----------|---------|----------------------------|
| Salvo | Rust | Über `BasicAuth`-Middleware und benutzerdefinierten `BasicAuthValidator` |
| Express | JavaScript | Verwendung des `express-basic-auth`-Middleware-Pakets |
| Spring Security | Java | Über `httpBasic()`-Konfiguration und `UserDetailsService` |
| ASP.NET Core | C# | Verwendung von `.UseAuthentication()` und `AddAuthentication(AuthenticationSchemes.Basic)` |
| Gin | Go | Über benutzerdefinierte Middleware oder das `gin-contrib/auth`-Paket |

## Anwendungsfälle

Basic Auth eignet sich für folgende Szenarien:

1. **Interne APIs und Tools**: Innerhalb eines Unternehmens genutzte Verwaltungstools und APIs
2. **Entwicklungs- und Testumgebungen**: Schnelle Implementierung von Authentifizierung ohne komplexe Anmeldesysteme
3. **Einfacher API-Schutz**: Wenn kein komplexes Benutzerverwaltungssystem benötigt wird
4. **In Kombination mit anderen Sicherheitsmaßnahmen**: Als Teil einer mehrschichtigen Sicherheitsarchitektur

In Salvo kann die Basic-Auth-Middleware einfach in Routen integriert werden. Durch Implementierung des `BasicAuthValidator`-Traits lässt sich die Validierungslogik flexibel anpassen.

## Hinweise

- Immer in Kombination mit HTTPS verwenden, um die Übertragung der Anmeldedaten zu schützen
- Für Produktionsumgebungen mit sensiblen Daten nicht geeignet
- Für Produktionsumgebungen sollten sicherere Authentifizierungsmethoden wie JWT oder OAuth in Betracht gezogen werden

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/basic-auth/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/basic-auth/Cargo.toml"
  ```
  </Tab>
</Tabs>
{/* Auto generated, origin file hash:d01ddb1691782cd7ed28227dcc89e19f */}