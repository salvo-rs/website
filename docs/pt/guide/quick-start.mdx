import { Tab, Tabs } from '@rspress/core/theme';

# Início Rápido

## Instalar Rust
Se ainda não tem o Rust instalado, pode usar a ferramenta oficial (`rustup`)[https://doc.rust-lang.org/book/ch01-01-installation.html] para instalar o Rust.

:::tip
A versão mínima do Rust suportada atualmente pelo Salvo é 1.85. Execute `rustup update` para confirmar que tem uma versão compatível do Rust instalada.
:::

## Escrever o Primeiro Programa em Salvo

Crie um novo projeto:

```bash
cargo new hello --bin
```

Adicione as dependências ao `Cargo.toml`

```toml file="<root>/codes/hello/Cargo.toml"
```

No ficheiro `main.rs`, crie um manipulador de função simples chamado `hello`, que apenas imprime o texto `"Hello world"`.

```rust file="<root>/codes/hello/src/main.rs"
```

Parabéns, o seu primeiro programa em Salvo está completo. Basta executar `cargo run` na linha de comandos e depois abrir `http://127.0.0.1:5800` no seu navegador.

## Explicação Detalhada

Aqui, `hello_world` é um `Handler`, usado para processar os pedidos do utilizador. O `#[handler]` permite que uma função implemente facilmente o trait `Handler`. Além disso, permite-nos abreviar os parâmetros da função de diferentes formas.

- Forma original:

    ```rust
    #[handler]
    async fn hello(_req: &mut Request, _depot: &mut Depot, res: &mut Response, _ctrl: &mut FlowCtrl) {
        res.render("Hello world");
    }
    ```

- Pode omitir alguns parâmetros da função que não são utilizados. Por exemplo, `_req`, `_depot` e `_ctrl` não são usados aqui, por isso podem ser omitidos:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) {
        res.render("Hello world");
    }
    ```

- Qualquer tipo pode ser usado como tipo de retorno da função, desde que implemente o trait `Writer`. Por exemplo, `&str` implementa `Writer`; quando é retornado, imprime texto simples:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> &'static str {
        "Hello world"
    }
    ```

- É mais comum precisarmos usar `Result<T, E>` como tipo de retorno para lidar com erros durante a execução da função. Se tanto `T` como `E` implementarem `Writer`, então `Result<T, E>` pode ser usado como valor de retorno:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> Result<&'static str, ()> {
        Ok("Hello world")
    }
    ```

## HTTP3 Elegante

Dizem que o HTTP3 é leve como uma pena, algo que muitos programadores desejam mas não conseguem alcançar. Desta vez, o Salvo realiza esse desejo, permitindo que todos desfrutem facilmente dos maravilhosos serviços que o HTTP3 oferece!

Primeiro, ative a funcionalidade HTTP3 no `Cargo.toml` e depois altere o `main.rs` da seguinte forma:
<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/hello-h3/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/hello-h3/Cargo.toml"
  ```
  </Tab>
</Tabs>

## Ferramenta CLI do Salvo 🛠️

O [Salvo CLI](https://github.com/salvo-rs/salvo-cli) é uma ferramenta projetada para o framework web Salvo, capaz de criar código limpo e legível, poupando-lhe tempo para coisas mais interessantes na vida.

Se tiver ideias para melhorar o CLI ou encontrar algum problema que precise de ser resolvido, não hesite! Submeta uma issue; as suas contribuições são bem-vindas.

### Passo 1

Instale a ferramenta CLI:

```bash
cargo install salvo-cli
```

### Passo 2

> Crie um novo projeto Salvo usando o comando `new` seguido do nome do seu projeto:
>
> ```bash
> salvo new nome_do_projeto
> ```

Com esta simples ferramenta CLI, pode iniciar rapidamente um projeto Salvo, concentrando-se na implementação da sua lógica de negócio em vez de na configuração da estrutura do projeto. ✨

## Mais Exemplos

Recomenda-se clonar diretamente o repositório do Salvo e depois executar os exemplos no diretório `examples`. Por exemplo, o seguinte comando executa o exemplo `hello`:

```sh
git clone https://github.com/salvo-rs/salvo
cd salvo/examples
cargo run --bin example-hello
```

Existem muitos exemplos no diretório `examples`. Todos podem ser executados com um comando semelhante a `cargo run --bin example-<nome>`.
{/* Auto generated, origin file hash:0c4a88ceb8e2d80be891a10caa082999 */}