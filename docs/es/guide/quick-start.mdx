import { Tab, Tabs } from '@rspress/core/theme';

# Comenzar Rápidamente

## Instalar Rust
Si aún no tienes Rust instalado, puedes usar la herramienta oficial (`rustup`)[https://doc.rust-lang.org/book/ch01-01-installation.html] para instalarlo.

:::tip
La versión mínima de Rust compatible con Salvo actualmente es la 1.85. Ejecuta `rustup update` para confirmar que tienes instalada una versión adecuada de Rust.
:::

## Escribe tu primer programa en Salvo

Crea un proyecto nuevo:

```bash
cargo new hello --bin
```

Añade las dependencias en `Cargo.toml`

```toml file="<root>/codes/hello/Cargo.toml"
```

En `main.rs`, crea un manejador de función simple llamado `hello`, que simplemente imprime el texto `"Hello world"`.

```rust file="<root>/codes/hello/src/main.rs"
```

¡Felicidades! Tu primer programa en Salvo está listo. Solo tienes que ejecutar `cargo run` en la línea de comandos y luego abrir `http://127.0.0.1:5800` en tu navegador.

## Explicación Detallada

Aquí, `hello_world` es un `Handler`, utilizado para procesar las solicitudes del usuario. El atributo `#[handler]` permite que una función implemente fácilmente el trait `Handler`. Además, nos permite abreviar los parámetros de la función de diferentes maneras.

- Forma original:

    ```rust
    #[handler]
    async fn hello(_req: &mut Request, _depot: &mut Depot, res: &mut Response, _ctrl: &mut FlowCtrl) {
        res.render("Hello world");
    }
    ```

- Puedes omitir los parámetros que no uses en la función. Por ejemplo, aquí `_req`, `_depot` y `_ctrl` no se utilizan, así que puedes no escribirlos:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) {
        res.render("Hello world");
    }
    ```

- Cualquier tipo puede ser el tipo de retorno de la función, siempre que implemente el trait `Writer`. Por ejemplo, `&str` implementa `Writer`; cuando se devuelve como valor, imprime texto plano:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> &'static str {
        "Hello world"
    }
    ```

- Es más común necesitar usar `Result<T, E>` como tipo de retorno para manejar errores durante la ejecución de la función. Si tanto `T` como `E` implementan `Writer`, entonces `Result<T, E>` puede ser un tipo de retorno válido:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> Result<&'static str, ()> {
        Ok("Hello world")
    }
    ```

## El Elegante HTTP3

Se dice que HTTP3 es ágil y ligero, un anhelo de muchos desarrolladores. ¡Esta vez, Salvo cumple ese deseo, permitiéndote disfrutar fácilmente de los maravillosos servicios que ofrece HTTP3!

Primero, habilita la funcionalidad HTTP3 en `Cargo.toml`, y luego modifica `main.rs` de la siguiente manera:
<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/hello-h3/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/hello-h3/Cargo.toml"
  ```
  </Tab>
</Tabs>

## Herramienta CLI de Salvo 🛠️

[Salvo CLI](https://github.com/salvo-rs/salvo-cli) es una herramienta diseñada para el framework web Salvo. Puede generar código limpio y legible, ahorrándote tiempo para las cosas más interesantes de la vida.

Si tienes ideas para mejorar la CLI o encuentras algún problema que resolver, ¡no dudes en hacerlo! Abre un issue; tus aportes son bienvenidos.

### Paso 1

Instala la herramienta CLI:

```bash
cargo install salvo-cli
```

### Paso 2

> Crea un nuevo proyecto de Salvo usando el comando `new` seguido del nombre de tu proyecto:
>
> ```bash
> salvo new nombre_del_proyecto
> ```

Con esta sencilla herramienta CLI, puedes iniciar rápidamente un proyecto de Salvo, centrándote en implementar tu lógica de negocio en lugar de en configurar la estructura del proyecto. ✨

## Más Ejemplos

Se recomienda clonar directamente el repositorio de Salvo y luego ejecutar los ejemplos en el directorio `examples`. Por ejemplo, el siguiente comando ejecuta el ejemplo `hello`:

```sh
git clone https://github.com/salvo-rs/salvo
cd salvo/examples
cargo run --bin example-hello
```

Hay muchos ejemplos en el directorio `examples`. Todos pueden ejecutarse con comandos similares a `cargo run --bin example-<nombre>`.
{/* Auto generated, origin file hash:0c4a88ceb8e2d80be891a10caa082999 */}