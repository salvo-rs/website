# 壓縮回應

用於處理 `Response` 內容壓縮的中介軟體。

支援三種壓縮格式：`br`、`gzip`、`deflate`。可根據需求配置各壓縮方式的優先順序等參數。

## HTTP 協定中的資料壓縮

1. 檔案格式層面：特定檔案類型使用專用壓縮演算法
2. HTTP 協定層面：透過內容編碼實現端到端的資源壓縮傳輸
3. 連線層面：在 HTTP 連線的節點之間進行資料壓縮

更多關於 HTTP 壓縮的資訊可參考 [Mozilla 開發者網路文件](https://developer.mozilla.org/zh-TW/docs/Web/HTTP/Guides/Compression)。

## 範例程式碼

import { Tab, Tabs } from '@rspress/core/theme';

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/compression/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/compression/Cargo.toml"
  ```
  </Tab>
</Tabs>
{/* 本行由工具自动生成,原文哈希值:adfd8752c21601fac83a31d329cf6dae */}