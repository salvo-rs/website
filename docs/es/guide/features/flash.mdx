# Flash

Un middleware que proporciona funcionalidad de Mensajes Flash.

`FlashStore` ofrece operaciones de almacenamiento y recuperación de datos. `CookieStore` almacena datos en `Cookies`, mientras que `SessionStore` almacena datos en `Session`. `SessionStore` debe utilizarse junto con la función `session`.

_**Código de Ejemplo**_  

### Ejemplo de Almacenamiento en Cookies

import { Tab, Tabs } from '@rspress/core/theme';

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/flash-cookie-store/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/flash-cookie-store/Cargo.toml"
  ```
  </Tab>
</Tabs>

### Ejemplo de Almacenamiento en Sesión

<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/flash-session-store/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/flash-session-store/Cargo.toml"
  ```
  </Tab>
</Tabs>
{/* Auto generated, origin file hash:6f1c773ac258fa165c7c1b6ffb218131 */}