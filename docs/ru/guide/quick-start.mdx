import { Tab, Tabs } from '@rspress/core/theme';

# Быстрый старт

## Установка Rust
Если у вас ещё не установлен Rust, вы можете воспользоваться официальным инструментом (`rustup`)[https://doc.rust-lang.org/book/ch01-01-installation.html] для его установки.

:::tip
Текущая минимальная поддерживаемая версия Rust для Salvo — 1.85. Выполните команду `rustup update`, чтобы убедиться, что у вас установлена подходящая версия.
:::

## Написание первой программы на Salvo

Создайте новый проект:

```bash
cargo new hello --bin
```

Добавьте зависимости в `Cargo.toml`

```toml file="<root>/codes/hello/Cargo.toml"
```

В `main.rs` создайте простой обработчик функции с именем `hello`, который просто выводит текст `"Hello world"`.

```rust file="<root>/codes/hello/src/main.rs"
```

Поздравляем, ваша первая программа на Salvo готова. Просто запустите `cargo run` в командной строке, а затем откройте в браузере `http://127.0.0.1:5800`.

## Подробное объяснение

Здесь `hello_world` — это `Handler`, предназначенный для обработки пользовательских запросов. `#[handler]` позволяет функции легко реализовать трейт `Handler`. Кроме того, он позволяет нам сокращённо записывать параметры функции разными способами.

- Исходная форма:

    ```rust
    #[handler]
    async fn hello(_req: &mut Request, _depot: &mut Depot, res: &mut Response, _ctrl: &mut FlowCtrl) {
        res.render("Hello world");
    }
    ```

- Вы можете опустить неиспользуемые параметры функции. Например, здесь `_req`, `_depot`, `_ctrl` не используются, поэтому их можно не писать:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) {
        res.render("Hello world");
    }
    ```

- Любой тип может быть возвращаемым типом функции, если он реализует трейт `Writer`. Например, `&str` реализует `Writer`, и при возврате он выводит обычный текст:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> &'static str {
        "Hello world"
    }
    ```

- Более распространённый случай — использование `Result<T, E>` в качестве возвращаемого типа для обработки ошибок в процессе выполнения функции. Если `T` и `E` оба реализуют `Writer`, то `Result<T, E>` может быть возвращаемым значением:

    ```rust
    #[handler]
    async fn hello(res: &mut Response) -> Result<&'static str, ()> {
        Ok("Hello world")
    }
    ```

## Элегантный HTTP3

Говорят, HTTP3 легок как пух, и многие программисты мечтают о нём, но не могут его заполучить. На этот раз Salvo исполняет ваши желания, позволяя легко насладиться прекрасным сервисом, который предоставляет HTTP3!

Сначала включите функцию HTTP3 в `Cargo.toml`, а затем измените `main.rs` следующим образом:
<Tabs>
  <Tab label="main.rs">
  ```rust file="<root>/codes/hello-h3/src/main.rs"
  ```
  </Tab>
  <Tab label="Cargo.toml">
  ```toml file="<root>/codes/hello-h3/Cargo.toml"
  ```
  </Tab>
</Tabs>

## Инструмент Salvo CLI 🛠️

[Salvo CLI](https://github.com/salvo-rs/salvo-cli) — это инструмент, разработанный для веб-фреймворка Salvo. Он позволяет создавать чистый, легко читаемый код, экономя ваше время для более интересных вещей в жизни.

Если у вас есть идеи по улучшению CLI или вы обнаружили проблемы, которые нужно решить, не стесняйтесь! Создайте issue, мы будем рады вашим предложениям.

### Шаг 1

Установите инструмент CLI:

```bash
cargo install salvo-cli
```

### Шаг 2

> Создайте новый проект Salvo, используя команду `new`, за которой следует имя вашего проекта:
>
> ```bash
> salvo new project_name
> ```

С помощью этого простого инструмента CLI вы можете быстро запустить проект Salvo, сосредоточившись на реализации бизнес-логики, а не на настройке структуры проекта. ✨

## Другие примеры

Рекомендуется клонировать репозиторий Salvo и запустить примеры из каталога `examples`. Например, следующая команда запускает пример `hello`:

```sh
git clone https://github.com/salvo-rs/salvo
cd salvo/examples
cargo run --bin example-hello
```

В каталоге `examples` много примеров. Все они могут быть запущены с помощью команд вида `cargo run --bin example-<name>`.
{/* Auto generated, origin file hash:0c4a88ceb8e2d80be891a10caa082999 */}